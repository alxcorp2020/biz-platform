# 업데이트 로그

이 파일은 굵직한 기능 단위로만 기록한다(세부 버그 수정/디자인 phase 등은
제외 — 그런 건 git log로 충분하다). 새 항목은 위(최신순)에 추가한다.

관례: 큰 기능을 완료할 때마다 아래 형식으로 오늘 날짜 섹션(없으면 새로
만들고)에 한 줄 추가한다.

```
## YYYY-MM-DD
- 기능 요약 한 줄
```

## 2026-08-04
- **Phase UX-04: 정보부족 비차단 표시 + 재무/업력 구간입력 + 정책 재확인**: Phase UX-03(단계별 카드) 요청을 다시 받았는데 조사해보니 1~4/10번은 이미 그 커밋(ac5a1b1)으로 구현·검증 완료 상태였음 — 재구현 없이 새로 나온 5·6·7·8·9번만 처리. **(1) 정보부족 시 공고추천 비차단**: 원래 공고 목록/상세는 프로필 완성도와 무관하게 항상 열람 가능해 차단 자체가 없었음(재확인만). 다만 0~30%/30~70%/70%+ 3단계로 판정 "방식"이 갈리는 로직은 없었어서 — `overallCompleteness<30%`일 때만 판정 로직(scoreNoticeForCompany)은 그대로 두고 **표시 레이어에서만** 즉시참여/조건보완/참여어려움 3분류 대신 "🔵 확인 필요 {total}건" 하나로 묶고 눈에 띄는(차단 아닌) 경고색 안내를 추가(`eligibilitySummaryRowsHtml` 신규 공유 헬퍼, 대시보드 카드+온보딩완료 화면 둘 다 적용). 30~70%/70%+ 두 구간은 기존 필드별 insufficient_data 메커니즘이 이미 자연스럽게 정밀도가 올라가는 효과를 내고 있어 추가 로직 없음(사용자 확인). **(2) 매출액/업력 구간 select**: 매출액은 AI가 문서에서 못 찾았을 때(candidate 비어있음)만 7단계 구간 select로 대체하고 confidence를 최하 등급 D로 낮춤(`company_financials.go`에 `revenueIsEstimated` 요청 필드 신규 — AI가 실제로 찾았으면 기존처럼 정밀 입력 유지). 업력은 애초에 AI 추출 대상이 아니라 항상 직접입력이었어서 그냥 7단계 구간 select로 전환(confidence 개념 자체가 없는 필드라 별도 표시 없음) — 업종별 세분화는 이번 범위에서 보류하고 코드 주석으로만 남김(사용자 확인). **(3) 조사 결과**: 신용등급은 프로필 직접입력(`company_profiles.credit_rating`)과 재무정보 AI추출(`company_financials.credit_rating`) 두 곳에 동기화 없이 따로 있음을 확인 — 카드 설계엔 재무정보 카드에만 맡기고 별도 카드는 안 만들기로(원래도 카드 큐 후보가 아니었음, 코드 변경 불필요). 재시도방지 정책(같은 파일 1시간 3회 실패 차단)은 사업자등록증만 빠져있고 나머지 4개(재무제표/4대보험/면허·인증/수행실적)는 공용 진입점을 통해 전부 적용돼 있음을 확인 — 사업자등록증은 회사 프로필이 생기기 전이라 구조적으로 이 메커니즘을 못 쓰고, 대신 이미 있는 `authLookupRateLimited`(userID 기준 1분1회+1일5회, 파일 무관)가 더 보수적이라 충분하다고 판단(사용자 확인, 코드 변경 없이 주석으로 이유만 남김). 로컬 검증: `overallCompleteness=15%` 계정으로 "확인 필요" 통합표시+경고배너 렌더링 확인, 공고 목록 API가 프로필 상태와 무관하게 항상 200 확인, `revenueIsEstimated:true`로 POST 시 confidence="D" 응답 확인(정밀 입력 시 기존처럼 B/C 유지도 재확인), 업력/매출액 구간 select 옵션 전체를 헤드리스 브라우저로 확인.
- **Phase UX-03: 단계별 정보요청 카드 + 즉시 보상**: `#/onboarding/complete`가 4버킷 요약을 정적으로만 보여주던 것에서, 그 아래 정보요청 카드를 하나씩 순차 노출하는 흐름으로 전면 재설계. 판정 로직(eligibility.go/scoring.go)은 이번에도 전혀 안 건드림. **(1) 카드 순서**: 지역/업종/기업규모 중 실제로 빈 필드(영향 건수 내림차순) 먼저, 그 다음 실적/인증/직접생산확인(요구서류 건수 내림차순) — 앞 3개만 큐로. `dashboard.go`의 `eligibilityBucketSummary`에 `missingFieldCounts map[string]int` 신규 추가(기존 판정 결과를 필드별로 더 세밀하게 집계할 뿐, 새 판정기준 아님). **(2) PC 모달 vs 모바일 전체화면**: 768px 기준으로 클릭 시점에 분기 — 이런 "모달-vs-라우트" 분기가 코드베이스에 처음이라 새로 만듦. 모바일은 신규 라우트 `#/onboarding/complete/register/{fieldKey}`(정규식 매치)로 전체화면 이동 후 저장 완료 시 `#/onboarding/complete`로 복귀 — 모듈 스코프 `onboardingCardQueueState`로 큐 진행상태(몇 번째 카드/누적 카운트)가 라우트 왕복에도 안 끊기게 함. **(3) 실제 재계산**: 지역/업종/기업규모는 등록 즉시 `GET /api/dashboard`를 다시 불러 `ready+needsReview` 전/후 실제 차이를 "현재 추천공고 N건 → M건 (+K)"로 표시(하드코딩 아님, 델타 0/음수도 자연스럽게 처리). 실적/인증/직접생산확인은 판정기준 자체가 없어 델타를 내지 않고 "✓ 등록 완료 — 앞으로 이 서류를 요구하는 판정에 반영됩니다"로 마무리(사용자 확인 완료 사항). 큐(최대 3장) 소진 후 마무리 화면: 갭이 전부 해소됐으면 "모든 정보를 확인했습니다!", 남아있으면 "~정보가 없어 N건을 아직 판정하지 못했습니다... [완료]/[처음부터]". **(4) 버그 발견·수정**: `computeDocumentRequirementGaps`의 "직접생산확인" 카테고리가 `company_licenses`에 아무 행이나(카테고리 필터 없이) 있으면 충족된 걸로 잘못 체크되고 있었고, 실제 `#/me/profile`의 "직접생산확인" 체크박스(`company_profiles.direct_production_cert`)는 이 계산과 연결이 안 돼있던 기존 버그(Phase UX-02 때부터) — 이번에 발견해 그 컬럼값을 직접 보도록 수정. **(5) "AI 분석 커버리지" 명칭 전체 스윕**: 이전 세션에 온보딩 흐름 2곳만 바꿨던 것을 랜딩페이지 소개문구/이미지 alt·성장분석 추이차트 제목 3곳·벤치마크 라벨까지 총 7곳 전부 통일(계산 로직/JSON 필드명/CSS클래스명/CHANGELOG 과거기록은 그대로 — 표시 문구만). 지역/업종/기업규모 미니 등록폼(`renderRegionQuickForm` 등) 3개는 `PUT /api/me/company-profile`이 부분수정이 아니라 항상 전체 컬럼을 덮어쓰는 UPDATE라는 점에 유의해 `currentProfile` 전체를 다시 실어 보내는 공용 헬퍼(`companyProfileUpdatePayload`) 만들어 안전하게 처리. 실적/인증 카드는 기존 컨테이너 비의존적 폼(`renderTrackRecordForm`/`renderLicenseConfirmForm`)을 그대로 재사용. 로컬 검증: `missingFieldCounts`가 실제 DB 재계산 값과 일치, `direct_production_cert` 토글 전/후(및 옛 버그 재현용으로 `company_licenses`에 무관한 행을 넣어도 안 속는 것까지) 확인, 로컬 apiserver+헤드리스 브라우저로 PC(1280px, 모달)/모바일(390px, 전체화면) 양쪽 전체 흐름(판정가능형 델타/일반안내형 확정문구/무시/마무리 화면 두 버전)과 767px↔768px 경계 정확히 갈리는 것까지 확인.
- **🚨 회원 삭제(완전삭제) 500 에러 긴급 수정 + 회원가입 필수(*) 표시 통일**: (1) 관리자 회원 상세 "회원 삭제"(`DELETE /api/admin/members/{id}`, 완전삭제/하드삭제) 클릭 시 500 에러 — 로컬 재현 결과 원인은 `password_reset_tokens`/`email_verification_tokens` 두 테이블이 `handleAdminDeleteMember`의 개인 데이터 삭제 목록(`userScopedStmts`)에서 빠져 있던 것. 둘 다 users(id)를 NOT NULL FK로 참조하고 CASCADE가 아닌데, 이메일 가입 필수 인증(email_verification.go)이 모든 이메일 가입 계정에 email_verification_tokens 행을 남기고 비밀번호 찾기를 한 번이라도 쓴 계정은 password_reset_tokens 행도 남아 — 사실상 대부분의 실제 계정에서 재현되는 흔한 버그였다(실제 로컬 재현: `violates foreign key constraint "password_reset_tokens_user_id_fkey"`). 두 테이블 + 같은 이유로 위험한 `broadcast_messages.created_by`(공지 작성 이력이 있는 관리자 계정 삭제 시 동일 문제)까지 삭제 목록에 추가해 수정. **"탈퇴 처리"(비활성화+익명화, `POST .../deactivate`)는 원래 설계·구현이 맞고 버그 없음을 로컬 재현으로 재확인**(users 행을 지우지 않고 email/password_hash/phone_number만 익명화 — 이 FK들과 무관). 로컬 Postgres에 실제 이메일 인증/비밀번호 재설정 토큰을 가진 테스트 계정을 만들어 수정 전(500 재현) → 수정 후(200, 실제 users 행 삭제 확인) 검증, 탈퇴 처리도 별도 테스트 계정으로 익명화 결과(이메일 `deleted-{id}@deleted.local`, password_hash/phone_number NULL, deactivated_at 설정) 확인. ⚠️Render 프로덕션 로그는 API 접근 권한이 없어 직접 조회하지 못했음 — 로컬 재현이 코드 레벨의 명확한 FK 누락이라 동일 원인일 가능성이 높지만, 배포 후 실제로도 해결됐는지 재확인 필요. (2) 이메일 회원가입 폼(`renderSignup`)의 이메일/휴대폰번호/비밀번호/비밀번호 확인 4개 필드에 `*` 필수 표시 추가(`.required-mark`, 공용 `otpPhoneFieldHtml`도 함께 수정돼 소셜로그인 온보딩의 휴대폰번호 필드에도 자동 반영됨 — 소셜로그인은 이메일/비밀번호 필드 자체가 없어 그 둘은 애초에 표시 대상 아님). 검증 로직(`form.checkValidity() && phoneVerified`)은 이미 4개 전부 입력+휴대폰 OTP 인증 완료까지 요구하고 있었음을 확인(추가 수정 불필요) — 헤드리스 브라우저로 필드를 하나씩 채워가며 버튼이 마지막(OTP 인증 완료) 순간에만 활성화되는 것을 단계별로 확인.
- **디자인 정렬 버그(넓은 화면에서 섹션 타이틀 왼쪽 쏠림) + 무한스크롤 20건 카운터**: (1) 대시보드(`.dash-section-title`)/기업프로필(`.profile-tabs`)/진행중사업 상세(범용 `h3`)의 섹션 타이틀이 넓은 화면(약 1384px 이상, 사이드바+본문 1080px cap 이후 남는 폭이 생기는 지점)에서 왼쪽으로 쏠려 보인다는 신고 조사 — 원인은 각 타이틀이 `margin: 0 0 Npx`처럼 좌우 margin을 명시적으로 0으로 못박아둔 것이, 900px 이상 사이드바 레이아웃의 `main > * { max-width:1080px; margin-left:auto; margin-right:auto; }`(각 섹션을 1080px로 캡+가운데정렬하는 규칙)를 덮어써버린 것. 1280px에서는 본문 폭(1040px)이 아직 1080px cap보다 좁아 증상이 안 보이다가, 1920px 등 더 넓은 화면에서만 실제로 드러남(1280px만 보고는 재현 안 됨 — 1920px로 별도 확인). 세 곳 다 좌우 margin은 생략하고 위/아래 margin만 남기는 방식으로 수정, 범용 `h3` 규칙까지 고쳐 같은 문제를 가진 다른 화면들도 함께 해결됨. **로컬 검증**: getBoundingClientRect로 수정 전(margin-left:0px, 형제 요소들과 다름)→수정 후(margin-left:268px, 형제 요소들과 동일) 직접 대조, 1280px/1920px/모바일 3개 뷰포트 스크린샷 비교. (2) 공고검색(`#/notices`) 무한스크롤 — 예전에 요청했던 "20건 단위 구분 표기"가 실제로는 목록 맨 위의 고정 카운터 한 줄("N건 / 전체 M건 표시 중")로만 부분 반영되어 있었고, 목록 중간에 20건 묶음마다 끼워 넣는 구분 표기는 없었음 — `renderNoticeListItems`에 20건(`NOTICE_LIST_BATCH_SIZE`)마다 같은 문구의 구분줄(`.notice-list-batch-divider`)을 목록 안에 삽입하도록 추가(맨 위 고정 카운터는 유지). 로컬 apiserver+헤드리스 브라우저로 실제 34건 공고를 끝까지 스크롤해 "20건 / 전체 34건 표시 중" 구분줄이 정확히 20번째 항목 뒤에 한 번 삽입되는 것을 데스크톱/모바일 양쪽에서 확인.
- **Phase UX-02: AI 온보딩(사업자등록증 기반) UX 개선**: Phase UX-01로 만든 하드게이트 흐름 자체는 그대로 두고, "AI가 회사를 만들어주는 느낌"이 나도록 5단계 우선순위로 UX만 다듬었다. **(1) 안내문구+요약카드**: `renderOnboardingHero`/`renderBusinessRegistrationOnboarding`을 "공공사업 AI 비서는 사업자등록증을 분석하여 귀사에 맞는 사업을 찾아드립니다 — ✓회사정보 자동생성 ✓업종 자동분석 ✓추천공고 생성, 약 30초" 카피로 교체. AI 추출 직후 전체 편집폼을 바로 보여주던 것을 `renderBusinessRegistrationSummaryCard`(상호·지역·업종 한 줄 요약 + "네, 맞습니다"/"수정하기")로 교체 — "수정하기"를 눌러야만 기존 전체 편집폼(`renderBusinessRegistrationEditForm`)이 나온다. **(2) AI 분석중 화면**: 현재 백엔드가 완전 동기 호출 1건(SSE/폴링 인프라 없음, grep으로 확인)이라 서버가 중간 단계를 알려줄 방법이 없음 — 5개 라벨을 타이머로 순환시키면 그게 가짜 진행률이 되므로, 클라이언트가 실제로 아는 상태 전이 3개만(`bizRegAnalysisStepsHtml`: 문서 확인됨→AI 분석 중→결과 준비) 체크리스트로 표시. **(3) 첫 AI 결과 화면**: `renderOnboardingComplete`는 이미 대시보드로 바로 안 보내고 4버킷 판정 현황을 먼저 보여주는 구조라 스펙에 부합 — 회사명을 넣은 "🎉 {회사명}의 AI 회사 프로필을 만들었습니다" 헤더로 카피만 보강. **(4) 명칭 변경**: "프로필 완성도"를 온보딩 흐름에서 실제로 보이는 두 곳(대시보드 `renderGrowthSummaryHtml`, 기업프로필 `renderProfileCompletenessCard`)만 "AI 분석 커버리지"로 변경(계산 로직 무변경, 성장분석 Phase 3의 추이차트/벤치마크 라벨은 범위 밖이라 유지). **(5) 부족한 정보 안내(실적/인증/직접생산확인, 실제 수치)**: eligibility.go 판정엔진은 이 세 카테고리를 전혀 판단하지 못해(지역/업종/예산규모만 판단) 예전엔 정확한 건수를 낼 수 없었는데, 이미 실제로 채워져 있는 `required_documents`(공고별 실제 제출서류, 규칙+AI 추출로 이미 채워짐)를 키워드로 매칭해 "이 서류를 요구하는 열린 공고가 몇 건인가"라는 별도의 정직한 지표를 새로 계산(`dashboard.go` `computeDocumentRequirementGaps`, 판정 로직은 1바이트도 안 건드림). 회사가 해당 카테고리 데이터를 하나라도 이미 갖고 있으면 응답에서 제외. **로컬 검증 중 실제 버그 발견·수정**: 최초 키워드 `%인증%`이 "직접생산확인증명서"(확인+증명서) 안에 우연히 부분 문자열로 들어있어(한글은 띄어쓰기 없이도 오탐 가능) "직접생산확인" 요구 공고가 "인증" 카테고리에도 잘못 집계됨 — `%인증서%`(3글자)로 좁혀 해결. `go build/vet/test` 전부 통과, 로컬 Postgres(scratchpad, DB `biz_platform`)에 테스트용 `required_documents` 행을 직접 넣어 새 SQL 쿼리(키워드 매칭+회사측 보유 여부 체크) 정확성 확인(수정 전 오탐 재현 → 수정 후 카테고리별 정확히 1건씩 확인), 로컬 apiserver를 직접 띄워 로그인 후 `GET /api/dashboard`로 `documentRequirementGaps` 필드가 실제 등록된 서류요구를 정확히 반영하는 것까지 end-to-end 확인, 새 카피 4종이 서빙되는 HTML에 실제로 포함된 것 확인. 테스트로 넣은 DB 행은 검증 후 전부 삭제.
- **🚨 신규 가입자 전원 빈 화면 무한루프 긴급 수정**: 사용자가 "신규 이메일가입/SNS로그인하면 사이드바 외 콘텐츠가 빈 화면, 메뉴 클릭해도 이동 안 됨"을 보고. 원인: `route()`의 온보딩 게이트 3개(약관동의/이메일인증/사업자등록증)가 각자 "자기 화면"만 예외로 뒀는데, 두 게이트의 조건이 동시에 참인 계정(예: 이메일가입 직후=onboarded true·emailVerified false·프로필 없음, 또는 소셜가입 직후=onboarded false·emailVerified true·프로필 없음)은 게이트A가 보낸 화면을 게이트B가 "내 예외 아님"이라며 다른 화면으로, 그 화면을 다시 게이트A가 원래 화면으로 — **두 게이트가 서로 무한 핑퐁**시켜 route()가 실제 화면 렌더링(dispatch)에 영영 도달하지 못했다(사이드바는 부팅 시 한 번만 그려져서 정상으로 보임 — 더 헷갈리게 만든 요인). 신규 가입자 전원(이메일/소셜 불문)이 100% 겪는 치명적 버그였다. 세 게이트를 각자의 좁은 예외 목록 대신 "온보딩 흐름에 속한 화면 전체"를 담은 공용 목록(`AUTH_ONBOARDING_ROUTES`) 하나로 통일해 근본 수정. 로컬에서 두 시나리오(이메일가입 직후 상태, 소셜가입 직후 상태) 모두 실제로 무한루프 재현 조건을 만들어 검증 — 수정 후 각각 정확히 한 번만 리다이렉트되어 올바른 화면에 정착하고, 전체 온보딩 완료 후 다른 메뉴로도 정상 이동되는 것까지 확인. 별개로 서비스워커의 `cache.put()`이 브라우저 확장 프로그램의 `chrome-extension://` 요청까지 가로채다 예외를 내던 버그도 함께 발견해 수정(http/https 스킴이 아니면 그냥 통과시키도록).
- **🚨 운영 마이그레이션 크래시 긴급 수정**: 사용자가 Render 로그에서 `schema migration failed: ... auth_lookup_attempts_kind_check ... violated by some row` 에러(반복 발생, 서비스 부팅 불가)를 보고. 원인: `ensureAuthLookupKindEmailVerifyResend`가 `ensureAuthLookupKindBizRegExtract`보다 먼저 실행되는데, 전자가 옛날(biz_reg_extract 추가 전) 좁은 CHECK 목록으로 매 부팅마다 DROP+ADD를 반복해 — 실제로 `biz_reg_extract` 행이 쌓인 뒤에는 그 좁은 ADD CONSTRAINT 자체가 기존 행과 충돌해 실패, 서버가 아예 못 뜸(무한 크래시루프). 이 정확한 버그 클래스는 `notification_log_event_type_check`에서 과거에 이미 한 번 발생·수정된 이력이 있었는데(`ensureDeadlineD7EventType` 주석 참고), 이번에 admin_broadcast/password_reset/email_verification 3개를 추가하면서 "새 함수 하나만 추가하고 이전 함수들은 그대로 두는" 실수를 반복해 그 4개 함수(`ensureDeadlineD7EventType`/`ensureReportEventTypes`/`ensureTeamInviteEventTypes`/`ensureAdminBroadcastEventType`)도 전부 옛날 좁은 목록에 멈춰 있었음을 함께 발견 — 운영에 password_reset/email_verification 알림 기록이 이미 쌓여있다면 다음 재기동 때 똑같이 크래시했을 잠재 사고. `auth_lookup_attempts.kind`/`notification_log.event_type` 두 CHECK 제약을 건드리는 함수 전부(총 8개)를 항상 "현재 최종" 전체 누적 목록으로 동기화해 근본 수정. 로컬에서 정확히 같은 방식으로 재현(운영과 동일한 에러 메시지 확인) 후, 수정한 코드로 같은 "오염된" DB 상태에서 연속 재기동 2회 모두 정상 기동되는 것까지 확인. **다음에 이 constraint들에 새 값을 추가할 때는 새 함수 하나만 추가하지 말고 `grep "notification_log_event_type_check"` 등으로 전부 찾아 함께 넓힐 것**(migrate.go의 `ensureDeadlineD7EventType` 주석에 재발 이력으로 남겨둠).
- **사업자등록증 등록을 하드 게이트로 승격**: Phase UX-01 최초 구현 때는 "나중에 하기"로 건너뛸 수 있는 소프트 유도였는데, 사용자가 "안 하면 절대 이동 불가"로 명시적으로 바꿔달라고 요청 — 휴대폰/이메일 인증과 같은 강제성으로 승격시켰다. `route()`에 새 게이트(`COMPANY_PROFILE_EXEMPT_ROUTES`) 추가 — `currentProfile`(loadMe()가 이미 채워둔 전역 상태, 새 필드 불필요)이 없는 `role==='user'` 계정은 `#/onboarding/business-registration`/`#/onboarding/complete`/`#/terms`/`#/privacy` 외 모든 경로에서 강제로 그 화면으로 돌려보낸다. `renderBusinessRegistrationOnboarding` 화면에 휴대폰/이메일 인증 화면과 같은 스타일(사이드바 숨김, `is-auth`)을 적용하고, 이제 기능하지 않을 "나중에 하기" 버튼은 제거 — 대신 AI 추출이 반복 실패하거나 사용자가 그냥 직접 입력하고 싶을 때를 위한 "직접 입력하기" 링크(빈 확인폼을 열어 완전 수동 입력 가능)를 남겨, 하드 게이트라도 업로드 자체를 강제하지는 않고 "회사 정보 등록"만 강제한다. 로컬 검증: 헤드리스 브라우저로 신규 가입 계정이 `#/`·`#/notices`·`#/pipeline`·`#/me/profile`·`#/calendar` 등 어디로 이동을 시도해도 전부 온보딩 화면으로 돌아오는 것 확인, "직접 입력하기"로 완전 수동 입력→회사 생성→게이트 해제→정상 네비게이션 재개까지 end-to-end 확인.
- **Phase UX-01: 사업자등록증 OCR 자동 회사생성 + 대시보드 온보딩 UI**: 회원가입 이후 "회사 생성"을 사용자가 업종/기업규모/지역 등을 직접 입력하던 방식에서 "AI가 사업자등록증만으로 회사를 만들어주는" 방식으로 전환. **(1) 신규 파일 `business_registration.go`**: `POST /api/me/business-registration/extract`(로그인 필요, 회사 프로필 없어도 동작) — 사업자등록증(PDF/JPG/PNG/WEBP, HEIC는 이번 범위 제외)을 업로드하면 Claude Sonnet 5가 강제 tool-use로 사업자등록번호/상호/대표자/주소/지역/업종/업태를 추출해 candidate JSON만 응답(기존 6개 서류 카테고리와 동일 원칙 — DB에 저장하지 않음). 지역(17개 광역시도)·업종(기존 `industryGroups`)은 자유텍스트 매핑 없이 Claude tool schema의 enum을 강제해 AI가 직접 매핑하게 함(기존 면허추출기의 documentType enum 강제와 동일 패턴). `receiveCompanyDocument`(company_documents.go)는 회사 프로필 존재를 강제해 재사용 불가라 독립 엔드포인트로 신설, AI 한도/재시도 제한 대신 `auth_lookup_attempts`(신규 kind `biz_reg_extract`, 1분1회·1일5회)로 남용 방지. 기존 면허추출기의 documentType enum에서 "사업자등록증" 제거(신규 전용 플로우와 충돌 방지). **(2) `handleUpsertCompanyProfile`(auth.go) 확장**: 새 엔드포인트를 만드는 대신 이 기존 핸들러에 `businessRegistrationNumber`/`companyName`/`representativeName`/`address` 4개 필드를 추가해 그대로 재사용 — 사용자가 AI 추출 결과를 확인/수정 후 "예, 맞습니다"를 누르면 이 핸들러가 기존과 동일한 트랜잭션(프로필 생성+owner 멤버십)으로 저장한다. 마이그레이션 2파일(`db/migrations/001_init.sql`+`migrate.go`의 `ensureBusinessRegistrationColumns`)로 `company_profiles`에 4개 컬럼 추가. **(3) 대시보드 온보딩 UI**: `renderOnboardingHero`(회사 프로필 없는 로그인 사용자의 첫 홈 화면) 재설계 — "🎉 가입을 환영합니다" + 0% 진행률 바(`.doc-progress-bar` 기존 컴포넌트 재사용) + "사업자등록증 등록하기"(`#/onboarding/business-registration`, 신규 업로드+확인 화면)/"나중에 하기"(`#/notices`, 하드 게이트 아님) 버튼. 확정 후 `#/onboarding/complete`(신규)로 이동해 "🎉 사업자등록증 분석이 완료되었습니다" + 실제 계산된 4버킷 판정 현황을 보여줌. **(4) 4버킷 판정 집계(신규, 판정엔진은 안 건드림)**: `dashboard.go`에 `computeEligibilityBucketSummary` 추가 — 기존 `scoreNoticeForCompany`(eligibility.go/scoring.go, 무변경)를 그대로 재사용해 열려있는 공고 전체를 즉시참여가능/조건보완/참여어려움/**판정불가**(신규 분류, 회사측 정보부족으로 인한 insufficient_data) 4버킷으로 재분류. "판정불가" 사유는 지역/업종/기업규모 중 실제로 회사 프로필에 비어있는 필드를 동적으로 찾아 "OO 정보가 없어 N건을 정확하게 판정하지 못했습니다" 문구로 조합(판정엔진이 실제로 계산한 값이라 정확함). `GET /api/dashboard` 응답에 `eligibilitySummary` 필드 추가, 대시보드 완성도 카드 옆에 새 "추천공고 판정 현황" 카드로 표시. **의도적 범위 축소(사용자 확인 완료)**: 실적/인증/재무는 판정엔진에 기준 자체가 없어 "+N건" 같은 정확한 카운트를 낼 수 없음 — 이 세 항목은 완성도 점수 기반의 일반 안내 문구만 제공하고, 정확한 건수 계산이 필요한 새 판정기준 추가는 이번 범위에서 제외(사용자에게 명시적으로 확인받음). 기존 AI 분석엔진/DB 구조/추천엔진/관리자 기능/OCR 파이프라인 근간/공고 수집 로직은 전부 무변경. 로컬 검증: 마이그레이션 적용(신규 컬럼 4개+kind 확장) 확인, curl로 추출 엔드포인트(정상 요청 시 ANTHROPIC_API_KEY 미설정으로 인한 502 확인/미지원 파일형식 거부/내용불일치 거부/미인증 401/rate-limit 429), 확장된 `PUT /api/me/company-profile`(4개 신규 필드 저장+재조회 일치), `GET /api/dashboard`의 eligibilitySummary(4버킷 합계가 실제 열려있는 공고 수와 정확히 일치, 기업규모 입력 전/후 insufficientData→needsReview 이동 확인) 확인, 기존 면허 업로드 엔드포인트가 enum 변경 후에도 정상적으로 AI 한도 게이트까지 도달하는 것으로 회귀 확인, 헤드리스 브라우저로 온보딩 히어로→업로드 화면→확인 폼(AI 추출값 프리필)→확정→완료화면(실제 카운트 표시)→대시보드 판정현황 카드까지 전체 흐름 end-to-end 확인.
- **휴대폰 인증 사용여부 관리자 토글 + 이메일 가입 필수 이메일 인증 추가**: (1) `#/admin` 대시보드 "플랜 설정" 카드에 "회원가입 시 휴대폰 SMS 인증 사용" 체크박스 신설(`system_settings.phone_verification_required`, 기본값 `true` — 기존 동작 그대로 유지). 관리자가 끄면 `handleSignup`/`handleSignupAgreement`(이메일 가입/소셜로그인 온보딩 둘 다) 모두 휴대폰번호가 선택 입력이 되고 SMS 인증 없이도 가입 가능(값·형식 검증은 입력했을 때만 그대로 적용). 프론트는 공개 `GET /api/auth/config`로 이 값을 읽어 회원가입/소셜 온보딩 화면에서 OTP 발송·확인 UI(`otpPhoneFieldHtml`)를 보여줄지, 인증 절차 없는 선택 입력 하나(`plainPhoneFieldHtml`)만 보여줄지 결정한다. (2) 이메일 가입은 이제 필수 이메일 인증을 거친다 — 계정은 가입 즉시 생성/로그인되지만(`email_verification.go`), 인증 링크(`#/auth/verify-email?token=...`, 24시간 유효, SHA-256 해시 저장, 1회용)를 클릭하기 전까지는 `route()`가 대부분의 화면 접근을 막고 재발송 버튼이 있는 대기 화면(`#/auth/verify-email-pending`)으로 돌려보낸다. 소셜 로그인(구글/네이버/카카오)은 제공자가 이미 검증한 이메일이라 계정 생성 시점(`oauth_login.go`)에 바로 인증 완료 처리되어 이 게이트를 거치지 않는다. **"온보딩 완료" 판정 기준 재조정**: 휴대폰 인증이 선택이 될 수 있게 되면서 `phone_verified_at`만으로는 더 이상 온보딩 완료 여부를 판단할 수 없어져, `handleMe`가 `terms_agreements` 행 존재 여부로 계산한 `onboarded` 필드를 새로 내려주고 `route()`의 소셜로그인 온보딩 가드가 이 값을 쓰도록 전환(`phoneVerified` 자체는 정보용으로 유지). 재발송(`POST /api/me/resend-verification-email`)은 이미 인증된 계정이면 거부하고, [[아이디 찾기/비밀번호 찾기]] 기능에서 만든 `auth_lookup_attempts` 공용 남용방지 테이블에 `email_verify_resend` kind를 추가해 재사용(같은 계정 1분 1회, 1일 5회). 기존 회원 전원은 마이그레이션이 일괄 백필(`email_verified_at = created_at`)해 새 게이트의 영향을 받지 않음 — 이 백필은 컬럼이 "처음" 추가될 때 딱 한 번만 실행되도록 information_schema로 방어(안 그러면 매 서버 재시작마다 새로 가입한 미인증 사용자까지 자동으로 인증완료 처리되는 버그가 생김). 로컬 검증: 관리자 토글 on/off 전환 후 각각 회원가입(휴대폰 필수/선택) 성공 확인, 토글 off 상태에서 형식 오류 전화번호는 여전히 거부되는 것 확인, 이메일 인증 전체 흐름(가입→대기화면 강제이동→다른 화면 접근 차단→재발송 rate-limit→링크 클릭→인증완료→접근 허용) curl+헤드리스 브라우저로 end-to-end 확인, 기존 회원 9명 전원 `email_verified_at` 백필 확인, 관리자 체크박스 UI 클릭→저장까지 확인. **구현 중 발견해 함께 고친 연쇄 버그 2건**(휴대폰 인증을 선택으로 바꾸면서 파생됨): (1) `handleUpsertCompanyProfile`이 최초 회사 프로필 생성 시 `phone_verified_at IS NOT NULL`을 무조건 요구해, 토글을 끈 뒤 가입한(휴대폰 인증을 아예 안 거친) 회원은 회사 프로필을 영원히 못 만드는 상태가 될 뻔함 — 이 가드를 phoneRequired 설정값 조건부로 바꿔 고침. (2) `oauth_login.go`의 콜백 리다이렉트가 `phone_verified_at` 기준으로 온보딩 화면 이동을 판단해, 이미 약관동의까지 마쳤지만 휴대폰 인증은 한 적 없는(토글 off 상태에서 가입한) 소셜 로그인 회원이 로그인할 때마다 매번 온보딩 화면으로 튕기게 될 뻔함 — `terms_agreements` 존재 여부 기준으로 바꿔 고침(handleMe의 onboarded와 동일 기준). 두 버그 모두 로컬 curl 재검증(토글 off로 가입한 계정의 회사 프로필 생성 성공, 토글 on 계정은 기존과 동일하게 정상 동작) 완료.
- **아이디(이메일) 찾기 + 비밀번호 찾기 추가**: 로그인 화면에 "아이디/비밀번호 찾기" 링크(`#/auth/find-account`, 탭 2개)를 신설. **이메일 찾기**(`POST /api/auth/find-email`, `find_email.go`)는 가입 시 인증된 휴대폰번호로 계정을 조회해 이메일을 `ab***@domain.com` 형태로 마스킹해 보여준다(로컬파트 앞 2글자만 남기고 나머지는 원래 길이와 무관하게 항상 `*` 3개). 본인인증은 우선 "가입 시 등록한 휴대폰번호 입력"만으로 최소 구현(SMS 인증코드 재확인은 SMS 인프라 비용 문제로 이번엔 생략, 추후 추가 예정). **비밀번호 찾기**(`POST /api/auth/reset-password-request` + `POST /api/auth/reset-password`, `password_reset.go`)는 이메일 입력 → 계정이 존재하고 비밀번호가 있는(소셜로그인 전용 아닌) 경우에만 기존 Resend 인프라로 재설정 링크(`#/auth/reset-password?token=...`, 1시간 유효, SHA-256 해시로 저장, 1회용) 발송 → 링크 클릭 시 새 비밀번호 설정 화면. **anti-enumeration**: reset-password-request는 계정 존재/소셜전용 여부와 무관하게 항상 동일한 200 응답을 반환(표준 관행) — find-email은 반대로 "인증 후 표시"가 기능 스펙 자체라 계정 존재 여부가 결과에 그대로 드러난다. 두 조회 모두 OTP 챌린지가 없어 신규 `auth_lookup_attempts` 테이블로 phone_verifications와 같은 기준(같은 identifier 1분 1회, 1일 5회)의 남용 방지를 공용 적용(`authLookupRateLimited`). 신규 `password_reset_tokens` 테이블 추가, `notification_log.event_type` CHECK에 `password_reset` 추가. 세션이 HMAC 서명 쿠키라 서버측 세션 테이블이 없어 비밀번호 재설정이 다른 기기의 기존 세션을 무효화하지 않는 것은 기존 아키텍처 한계로 이번 범위에서 다루지 않음. 로컬 검증: curl로 이메일 찾기(정상/미가입/형식오류/rate-limit 429) 및 비밀번호 찾기 전 구간(정상 발송 로그·소셜전용 계정 제외·존재하지 않는 이메일과 응답 동일·토큰 무효/만료/재사용/짧은 비밀번호 거부·정상 재설정 후 새 비밀번호로 로그인 성공, 구 비밀번호 거부) 확인, 헤드리스 브라우저로 로그인 화면 링크→탭 전환→이메일 찾기 결과 표시→비밀번호 재설정 요청 안내 문구→토큰 포함 링크 접속→새 비밀번호 제출→완료 화면까지 end-to-end 확인.

## 2026-08-03
- **회원가입/온보딩 전면 재설계 Phase 1 — 업체정보 입력을 가입에서 완전히 분리, 휴대폰 SMS 인증 도입**: 사용자가 제시한 "회원가입 30초, 사업자등록증 등록까지 2분 이내 첫 추천공고" 목표에 따라 회원가입 화면에서 지역/업종/기업규모/사업자 대표전화 등 업체정보 관련 필드를 전부 제거하고, 이메일/휴대폰번호(SMS 인증)/비밀번호/비밀번호확인/약관동의만 남겼다(2단계 마법사 폐지 — `renderSignup` 단일 화면으로 통합). 소셜 로그인(구글/네이버/카카오)은 콜백이 계정을 이미 만들어버려 "계정 생성 시점에 함께" 처리가 안 되므로, 로그인 이후 별도의 최소 온보딩 화면(`renderOAuthOnboardingStep`, `#/auth/oauth-profile`)에서 휴대폰 인증+약관동의만 받는다 — 업체정보는 이 화면에서도 받지 않는다.
  - **휴대폰 SMS 인증 신규 구축**(`phone_verification.go`): 6자리 코드를 SHA-256 해시로 저장(`phone_verifications` 신규 테이블), `POST /api/auth/phone/send-code`/`verify-code` 공개 API 2개 추가. 재발송 남용 방지로 같은 번호 1분에 1회+1일 5회 제한, 틀린 코드 5회 누적 시 재발송 유도. 알리고(Aligo) 미설정 로컬 환경에서도 테스트 가능하도록 코드값을 서버 로그에 남김(운영 환경은 항상 설정돼있어 이 분기를 안 탐).
  - **"온보딩 완료" 판단 기준 전환**: `company_profiles`(회사 프로필) 존재 여부 → `users.phone_verified_at` 존재 여부로 변경. `oauth_login.go`의 리다이렉트 분기, `route()`의 강제 이동 가드, `handleUpsertCompanyProfile`의 최초 생성 가드(`phone_number_required`) 전부 이 기준으로 통일. 회사 프로필 생성은 이제 가입과 완전히 무관하게 아무 때나(수동 "기업 프로필 수정" 등으로) 가능.
  - `handleSignup`이 이제 `consumeVerifiedPhone`으로 실제 인증 여부를 서버가 재확인한 뒤(클라이언트 주장을 그대로 안 믿음, 결제금액 재계산과 같은 원칙) 계정 생성+휴대폰번호+약관동의를 한 트랜잭션에 기록. `signup_agreement.go`는 이제 소셜 로그인 온보딩 전용으로 축소.
  - 로컬 검증: send-code→재발송 1분 제한→틀린 코드 거부→올바른 코드 인증 전부 curl로 확인, 헤드리스 브라우저로 이메일 가입(OTP 발송~인증 완료 표시~회원가입 완료) 전체 흐름과 소셜가입 시뮬레이션(계정만 있고 미인증 상태 → 다른 화면 접근 차단 확인 → 온보딩 화면에서 인증 완료 → 이후 자유 이동) 둘 다 끝까지 완료해 `phone_verified_at`/`terms_agreements` 정확히 기록되는 것 확인.
  - **다음 단계(Phase 2, 이번 범위 아님)**: 사업자등록증 업로드+AI(OCR) 추출+확인/수정 화면+회사 프로필 자동생성. 그 전까지는 가입 직후 회사 프로필이 없는 상태가 정상이며, 기존 홈 화면의 "온보딩 히어로" 안내와 "기업 프로필 수정" 모달(수동 입력)이 임시로 그 역할을 대신한다.
- **회원가입 2단계(업체정보) 필수항목 완화**: 바로 전에 지역/업종/기업규모/사업자 대표전화까지 전부 필수로 만들었던 것을, 가입 마찰이 너무 크다는 피드백에 따라 실제 필수 항목을 (소셜가입만 보이는)휴대폰번호 + 약관동의 2개로 되돌림 — 지역/업종/기업규모/사업자 대표전화는 선택 입력으로 전환(select/input에서 `required` 제거, 업종 "최소 1개" 검증도 제거). 서버(`handleUpsertCompanyProfile`)의 `business_phone_required` 체크도 제거(개인 휴대폰번호 `phone_number_required`는 유지). 빈 문자열이 그대로 저장되지 않도록 제출 시 `region`/`companySize`/`businessPhone`을 `|| null`로 정규화(기존 다른 프로필 수정 폼과 동일 패턴 재사용) — 실제로 DB에 NULL로 정확히 저장되는 것 확인. 안내 문구도 "이 3가지만 있으면..."에서 "휴대폰번호와 약관 동의만 하면 바로 가입할 수 있어요"로 갱신. 로컬 검증: 지역/업종/기업규모/사업자 대표전화를 전부 비운 채 휴대폰번호+약관 2개만 체크해도 버튼이 활성화되고 제출이 성공하는 것, 해당 필드들이 DB에 빈 문자열이 아니라 정확히 NULL로 저장되는 것, 이메일 가입 흐름(휴대폰번호 필드 자체가 없음)에서도 약관 체크만으로 활성화되는 것까지 확인.
- **회원가입 2단계(업체정보) 화면에서도 사이드바 숨김**: 1단계(계정정보)는 로그인 화면처럼 사이드바 없이 카드만 중앙에 뜨는데, 2단계(업체정보, 이메일/소셜 공통)는 지금까지 일반 사이드바 레이아웃으로 보였던 것을 1단계와 통일 — `renderSignupProfileStep`이 `is-auth`를 제거하던 것을 추가하도록 바꿈. 소셜 로그인 경로(`#/auth/oauth-profile`)는 `renderOAuthProfileStep`이 비동기(재차 `loadMe()` 호출)라 그 함수 내부에서만 클래스를 붙이면 대기 중 사이드바가 잠깐 보이는 깜빡임이 생길 수 있어, `route()`가 dispatch 직전 동기적으로 먼저 붙이도록 처리(기존 `is-landing` 깜빡임 방지와 같은 패턴). 로컬 검증: 헤드리스 브라우저로 이메일 가입 2단계·소셜 가입 2단계 둘 다 사이드바 없이 카드만 중앙에 뜨는 것, 가입 완료 후에는 사이드바가 정상적으로 돌아오는 것까지 확인.
- **(재적용) 소셜 로그인 미완료 가입 우회 차단**: 앞서 같은 날 추가(`41af342`)했다가 롤백(`d49c40b`)했던 가드를 다시 넣었다. 롤백 사유였던 "간편 로그인에 실패했습니다" 문제를 Render 로그로 직접 확인해보니 리다이렉트 URI 문제가 아니라 **테스트 중 "탈퇴 처리"된 계정으로 재로그인을 시도한 것**이었음이 밝혀져(`oauth-callback: deactivated account attempted login` — 정상 동작), 이 가드와는 무관한 별개 원인이었음을 확인. 원래 문제(로그인은 됐지만 2단계 "가입하기" 전까지 다른 화면으로 새서 휴대폰번호 누락 상태로 남을 수 있던 것)는 여전히 유효해 다시 적용 — 내용은 이전과 동일(`route()`에서 `currentUser && role==='user' && !currentProfile`이면 `#/auth/oauth-profile`/`#/terms`/`#/privacy` 제외 전부 강제 리다이렉트). 로컬 검증: 미완료 계정 `#/notices` 직접 접근 시 튕기는 것, `#/terms` 예외 유지, 가입하기 완료 후 자유 이동 전부 재확인.
- **회원가입 2단계(업체정보) 검증 로직 누락 수정**: "가입하기" 버튼이 이용약관·개인정보 체크박스 2개만 보고 활성화돼, 사업자 지역/업종/기업규모/휴대폰번호/사업자 대표전화가 비어있어도 눌리던 버그를 고쳤다. `updateSubmitEnabled`가 이제 `form.checkValidity()`(지역·기업규모 select의 required, 휴대폰번호·사업자 대표전화 input의 required+pattern을 그대로 재사용)와 업종 체크박스 최소 1개 선택 여부를 함께 확인해야 활성화되도록 변경, 폼 전체에 `input`/`change` 위임 리스너를 달아 아무 필드나 바뀔 때마다 재평가한다. 필수 필드 6개(지역/업종/기업규모/휴대폰번호[소셜가입만 노출]/사업자 대표전화/약관동의 2개) 라벨에 전부 "(필수)" 표시를 추가해 일관성을 맞췄다 — 안내문구("이 3가지만 있으면...")의 "3가지"는 지역/업종/기업규모(적합성 판정 규칙엔진이 쓰는 값)를 가리키는 것이었고, 사업자 대표전화는 애초 요청사항에 "필수"로 명시돼있던 필드라 안내문구와 별개로 필수 유지. 소셜 로그인(구글/네이버/카카오) 콜백 로직도 재검토했으나 신규가입이 2단계를 건너뛰는 경로는 없음을 재확인(`hasProfile` 조회 실패 시에도 안전하게 2단계로 보냄 — fail-closed). 로컬 검증: 헤드리스 브라우저로 6개 필수 필드를 하나씩 비웠을 때 전부 개별적으로 버튼을 비활성화시키는 것, 전화번호에 불완전한 값(패턴 불일치)을 넣었을 때도 비활성화 유지되는 것, 이메일 가입 흐름(휴대폰번호 필드 자체가 없는 2단계)에서는 그 필드 없이도 정상적으로 활성화되는 것까지 확인.
- **(추가 후 당일 롤백) 소셜 로그인 미완료 가입 우회 차단**: `route()`에 "로그인은 됐지만 업체정보(2단계)를 아직 안 마친 회원은 어느 경로로 이동하든 강제로 `#/auth/oauth-profile`로 돌려보낸다"는 가드를 추가했다가, 소셜 로그인 자체가 리다이렉트 URI 설정 문제로 막혀있던 상황과 겹쳐 원인 파악에 혼선을 줘 같은 날 제거함(커밋 41af342 도입 → 이후 커밋에서 되돌림). 이 가드가 실제 리다이렉트 URI 문제의 원인은 아니었음(둘은 무관 — 전자는 로그인 이후 프론트 라우팅, 후자는 로그인 시작 전 서버가 provider로 보내는 리다이렉트 주소 문제). 다시 필요해지면 `route()`의 로그인 가드(`isPublicRoute` 체크) 바로 뒤에 `currentUser && currentUser.role==='user' && !currentProfile` 조건으로 재추가하면 된다(과거 커밋 41af342 참고).
- **회원가입 휴대폰번호 입력 자동 하이픈 포맷**: 회원가입 1단계(계정정보)와 2단계(소셜 로그인 가입자용 휴대폰번호 입력란)에서 숫자만 입력해도 자동으로 하이픈이 붙도록 개선(`formatMobilePhoneInput`+`attachPhoneAutoFormat`, index.html). 010은 11자리(3-4-4: 010-1234-5678), 011/016/017/018/019(구 통신사 번호)는 10자리(3-3-4: 011-123-4567)로 자릿수 자체가 달라 첫 3자리(prefix)로 어느 쪽인지 판단해 포맷. `pattern` 속성과 서버측 검증 정규식(`signup_agreement.go`의 `phoneNumberPattern`)도 두 형식 모두 허용하도록 함께 갱신(기존엔 010 11자리만 허용해 그 외 번호는 가입 자체가 막혀 있었음). 사업자 대표전화번호(회사 대표번호, 랜드라인 등 형식이 다양함)는 이 자동포맷 대상에서 제외 — 개인 휴대폰번호 입력란에만 적용. 로컬 검증: node로 진행형 타이핑 시뮬레이션(010/011 각각 자릿수별 중간 상태) 확인, 헤드리스 브라우저로 실제 입력 필드에 키 입력 시뮬레이션 후 자동 포맷 스크린샷 확인, 011-123-2345로 회원가입 전체 흐름 완료해 DB에 정확히 저장되는 것까지 확인.
- **관리자 회원관리에 "회원 삭제"(완전 삭제) 추가**: 기존 "탈퇴 처리"(익명화, 데이터 보존)와 달리 개발 단계에서 반복 생성하는 테스트 계정을 흔적 없이 지우기 위한 하드 삭제(`DELETE /api/admin/members/{id}`, `handleAdminDeleteMember`). 대상이 회사(company_profiles)의 owner면 그 회사에 딸린 모든 데이터(문서/면허·인증/재무·실적·인력/파이프라인+체크리스트/담당자/구독/결제기록/리포트/알림이력 등, `company_documents`를 가리키는 `company_profiles.employee_count_source_document_id` 순환참조는 먼저 NULL로 끊음)까지 트랜잭션으로 함께 삭제. 안전장치 2가지: (1) 다른 팀원이 남아있는 조직의 owner는 삭제 거부(`owner_has_other_members`, 탈퇴 처리와 동일 정책 — 하드 삭제라 다른 팀원의 접근 권한이 예고 없이 사라지는 게 더 위험해서), (2) 관리자 자기 자신은 삭제 불가(`cannot_delete_self`, 복구 수단이 없어서). 프론트는 "탈퇴 처리" 버튼 옆에 "회원 삭제" 버튼을 추가하고, `confirm()` 하나로는 부족하다고 판단해 이메일 주소를 그대로 입력해야 진행되는 "타이핑 확인" 방식(`prompt()`)을 추가 안전장치로 둠. 로컬 검증: 자기 자신 삭제 시도 400 차단, 팀원 있는 owner 삭제 시도 409 차단(팀원 먼저 삭제 후 재시도하면 성공), 정상 삭제 후 company_profiles/company_members가 정확히 0건인 것을 DB에서 직접 확인, 헤드리스 브라우저로 실제 버튼 클릭→prompt 입력→삭제 완료→회원목록으로 리다이렉트까지 end-to-end 확인.
- **회원가입 2단계 마법사 재구성(계정정보 → 업체정보) + 소셜가입 필수정보 우회 차단**: 기존엔 1단계(이메일/비밀번호)에서 바로 계정을 만들고 2단계(회사정보)에서 개인 휴대폰번호+약관동의를 받았는데, 이를 1단계=계정정보(휴대폰번호+이메일+비밀번호+비밀번호확인), 2단계="업체정보"(사업자 지역/업종/기업규모/사업자 대표전화번호+약관동의)로 재구성. **계정 생성 시점을 2단계 "가입하기"로 미룸**(1단계 "다음 단계"는 이제 클라이언트 검증만 하고 서버에 아무것도 안 남김) — 그 결과 2단계에서 "이전으로"로 1단계를 왕복해도 이메일 중복가입 걱정 없이 자유롭게 값이 유지된다(모듈 레벨 `signupStep1Data`/`signupStep2Data`로 왕복 보존, 팀 초대 경유 가입은 예전처럼 1단계에서 즉시 계정생성+2단계 스킵 유지). 사업자 지역은 자유 텍스트에서 기존 17개 광역시도+전국 select(`REGION_OPTIONS`)로, 기업 규모도 자유 텍스트에서 새 `COMPANY_SIZE_OPTIONS`(소상공인/소기업/중기업/중견기업/대기업, eligibility.go의 `소기업` 판정과 정확히 일치) select로 전환. 사업자 대표전화번호(회사 단위, 개인 휴대폰번호와 별개)는 새 필드로 `company_profiles.phone_number`에 저장(과거 "조직 알림용, 미사용" 코멘트가 붙어있던 컬럼을 이 용도로 재활용, 회사정보 수정 화면에도 같은 필드 추가). **소셜 로그인(구글/네이버/카카오) 가입자는 1단계를 거치지 않으므로**(OAuth 콜백이 계정을 이미 만들고 바로 2단계로 보냄) 개인 휴대폰번호를 물어볼 기회가 영영 없어지는 문제가 생겨, 2단계 화면이 `fromEmailStep1` 여부로 개인 휴대폰번호 입력란을 조건부 표시(이메일 가입자는 1단계 값을 그대로 제출에 실어 보내 다시 안 물어봄, 소셜 가입자는 반드시 입력). **서버측 방어 추가**: `handleUpsertCompanyProfile`(최초 생성 시점)이 `users.phone_number`(개인)와 요청의 사업자 대표전화 둘 다 없으면 각각 `phone_number_required`/`business_phone_required`로 거부 — UI가 항상 signup-agreement(전화번호+약관) → company-profile 순서로 호출하는 것과 무관하게, 어떤 경로(직접 API 호출 등)로도 "전화번호 입력 없이 회사 프로필(=가입완료 판정 기준인 company_members)이 생성"되는 걸 서버 레벨에서 막는다 — oauth_login.go의 `hasProfile` 판정이 company_members 존재 여부만 보므로 이 가드가 없으면 그 판정을 우회할 수 있었음. 로컬 검증: curl로 signup-agreement를 건너뛰고 바로 company-profile을 호출하면 이메일/소셜 가입 시나리오 둘 다 400으로 막히는 것 확인, 헤드리스 브라우저로 1→2→이전으로→2 왕복 시 두 단계 값 모두 유지되는 것, 이메일 가입 전체 흐름과 소셜가입 시뮬레이션(1단계 없이 2단계 진입 시 휴대폰번호 입력란이 뜨는 것) 각각 끝까지 완료해 DB에 개인 휴대폰번호+사업자 대표전화가 정확히 분리 저장되는 것 확인.
- **로그인/가입 화면 깜빡임(FOUC) 제거 + 카드 중앙정렬 수정**: 직전 라우트 접근제어 작업에서 나온 UI 버그 2건. (1) 페이지 로드/이동마다 사이드바 있는 레이아웃이 잠깐 보였다 사라지는 현상 — 원인 두 가지를 각각 수정. 정적 `<body>`에 처음부터 `app-booting` 클래스를 박아 앱 셸(사이드바/헤더/탭바/전역배너)을 `visibility:hidden`으로 가리고 그 위에 로딩 스피너 오버레이만 보이게 했다가, `await loadMe(); route();`가 완전히 끝난 뒤에야 같은 동기 틱에서 클래스를 제거(초기 로드 FOUC). `route()`가 `#/` 경로일 때 `renderHome`의 비동기 대시보드 API 응답(401 감지)을 기다리지 않고, 이미 확정된 `currentUser` 값으로 `is-landing` 여부를 그 자리에서 동기적으로 결정하도록 변경(네비게이션 시 깜빡임). (2) 로그인/가입 카드가 화면 오른쪽으로 치우쳐 보이던 버그 — `body.is-auth main#app`이 `display:flex`인데 `flex-direction`을 지정 안 해 기본값 `row`로 동작, 형제 요소였던 로고(`<a class="auth-page-logo">`)와 카드(`.auth-card`)가 나란히 배치되면서 전체 그룹이 중앙에 있어도 로고가 왼쪽·카드가 오른쪽으로 나뉘어 보였던 것. 로고를 카드 형제가 아니라 카드 안쪽 최상단으로 옮기고, `main#app`에 `flex-direction: column` 추가, `.auth-card`의 `border-radius`를 하드코딩 값(14px)에서 공용 토큰 `--radius-card`(20px)로, padding을 2rem→2.5rem로 확대, 배경을 `--bg`→`--bg`에서 `--bg-soft`로 내려가는 은은한 그라데이션으로 변경. 로컬 검증: 네트워크를 인위적으로 늦춘(`Network.emulateNetworkConditions`) 상태에서 로딩 중간 시점을 캡처해 셸이 전혀 안 보이고 스피너만 뜨는 것을 로그인 상태/비로그인 상태(랜딩) 양쪽 다 스크린샷으로 확인, 로그인/가입 카드가 화면 정중앙(수직+수평)에 로고-제목-폼 순서로 표시되는 것 확인.
- **라우트 접근제어 정리**: 비로그인 상태에서 `#/notices`(공고검색) 등 내부 화면이 그대로 노출되던 문제 수정. 조사 결과 `#/notices`는 서버(`handleListNotices`)가 원래 비로그인도 200을 주는 옵셔널 인증 설계인 데다, 프론트가 상태코드를 보존 안 하는 `fetchJSON`을 써서 401 감지 자체가 불가능한 구조라 실제로 데이터가 새고 있었음(`#/pipeline`/`#/calendar`/`#/growth` 등 나머지는 서버가 401을 강제하고 프론트도 리다이렉트 코드가 있어 결과적으로는 안전했으나, 각 render 함수가 API 응답을 받은 뒤에야 뒤늦게 리다이렉트하는 분산된 방식이었음). `route()` 함수 진입 시점에 화이트리스트(`PUBLIC_ROUTES`) 기반 가드를 추가해 렌더 시작 전에 즉시 차단하는 방식으로 통일 — 공개: `/`, `#/announcements`(+상세), `#/terms`, `#/privacy`, `#/auth/login`, `#/auth/signup`, `#/invite`, `#/billing/plans`(요금비교는 가입 전에도 봐야 함), `#/billing/fail`. 나머지 전부(`#/notices`, `#/pipeline`, `#/calendar`, `#/notifications`, `#/me/*`, `#/growth`, `#/admin/*` 등)는 비로그인 시 `#/auth/login`으로 즉시 리다이렉트. **선행 버그 수정**: `loadMe()`(비동기)가 `await` 없이 호출되고 바로 `route()`가 실행되던 부팅 순서 때문에, 가드를 그대로 추가하면 실제 로그인 사용자도 새로고침마다 한 박자 로그인화면으로 잘못 튕기는 레이스컨디션이 있어 `await loadMe(); route();` 순서로 고침.
- **로그인/회원가입 화면 심플화**: `body.is-auth`(랜딩페이지의 `is-landing`과 동일 원리) 적용 시 사이드바·헤더·탭바·프로필배너를 숨기고 `#app`을 화면 중앙(세로까지) 정렬, 카드 위에 브랜드 로고 추가. 회원가입 2단계(연락처+약관동의, 같은 `#/auth/signup` 해시를 유지한 채 컨테이너만 갈아끼우는 구조)는 의도적으로 일반 사이드바 레이아웃으로 되돌림 — 소셜 로그인 경유 2단계(`#/auth/oauth-profile`)와 레이아웃을 통일하기 위함.
- 사이드바 메뉴 순서(홈→공고검색→진행중사업→알림→캘린더→기업프로필→성장분석→구독관리→공지사항, 관리자는 구분선 아래 system_admin 전용) 확인 결과 이미 요청 순서와 일치해 변경 없음.
- 로컬 검증: 헤드리스 브라우저로 비로그인 `#/notices`/`#/pipeline`/`#/admin` 직접 접근 시 로그인화면 강제이동, `#/announcements`/`#/terms`/`#/billing/plans`/`#/invite`는 비로그인도 정상 접근, 로그인화면 심플 레이아웃(사이드바 숨김+로고+소셜버튼), 로그인 후 새로고침(완전 재접속)해도 레이스컨디션 없이 정상 유지, 사이드바 메뉴 순서, 관리자 계정의 사이드바 하단 구분선+관리자 메뉴 전부 확인
- 회원가입 1단계에 비밀번호 확인(재입력) 필드 추가 — 오타로 인한 비밀번호 오기재 방지. 서버로는 안 보내고(API는 여전히 email/password만 받음) 제출 직전 클라이언트에서 두 값 일치 여부만 검사, 불일치 시 "비밀번호가 일치하지 않습니다" 표시하고 다음 단계로 안 넘어감.
- **회원가입 개선(연락처+필수 약관동의) + 약관 관리 기능 신설**. (1) 회원가입 2단계(회사정보 화면, 이메일 가입자와 소셜 로그인 가입자 모두 거치는 유일한 공통 화면 — 1단계는 소셜 가입자가 안 거쳐서 여기 넣음)에 연락처(휴대폰, `010-XXXX-XXXX` 형식검증) + 이용약관/개인정보 수집이용 각각 별도 체크박스(둘 다 체크해야 제출 버튼 활성화, "보기" 링크는 새 탭으로 `#/terms`·`#/privacy` 열어서 입력값 유실 방지) 추가. 새 엔드포인트 `POST /api/me/signup-agreement`가 처리 — 기존 `PUT /api/me/company-profile`(가입 2단계와 "회사정보 수정" 화면이 공유하는 upsert API)에 안 넣고 분리한 이유는, 거기 넣으면 기존 회원이 프로필을 고칠 때마다 약관에 매번 동의해야 하는 흐름이 되기 때문. 약관 버전은 클라이언트 값을 안 믿고 서버가 그 순간의 활성 버전을 직접 조회해서 `terms_agreements`(user_id/terms_version/privacy_version/agreed_at/ip_address)에 기록(결제 금액 검증과 같은 원칙). (2) `legal_documents` 테이블(type/version/content/effective_date/is_active) 신설 — 관리자 화면 `#/admin/legal-documents`에서 이용약관·개인정보처리방침 각각 새 버전을 발행하면(같은 type의 기존 활성 버전을 비활성화 + 새 행 활성화를 한 트랜잭션으로) 즉시 `#/terms`·`#/privacy`(공개 API `GET /api/legal-documents/{type}`)에 반영되고, 이전 버전은 삭제되지 않고 이력으로 남는다. content 안의 `{brand_name}`/`{company_name}`/`{business_registration_number}`/`{representative_name}`/`{address}`/`{main_phone}`/`{contact_email}` 토큰은 조회 시점 `company_info` 값으로 자동 치환(회사정보 중복 관리 방지 — banners.go의 `{brand_name}` 토큰 치환과 같은 패턴). 문서 렌더링은 팝업용 `simpleMarkdownToHtml`보다 풍부한 전용 파서(`legalMarkdownToHtml`, 제목/목록/표/인용 지원)를 새로 만들어 처리. 랜딩페이지 footer의 "준비중" 처리였던 약관 링크도 실제 `#/terms`·`#/privacy`로 연결. (3) **초기 시드 콘텐츠(이용약관/개인정보처리방침 전문)를 개인정보보호위원회 표준 개인정보 처리방침 양식 구조(수집목적/보유기간/제3자제공/위탁/정보주체 권리/파기/안전성확보조치/쿠키/보호책임자/권익침해구제 등)를 따라 이 서비스가 실제 수집하는 정보(이메일/연락처/소셜로그인/사업자정보/업로드서류/결제정보/담당자연락처 등) 기준으로 작성했으나, 법률 전문가 검토를 거치지 않은 초안이다** — 문서 최상단(사용자 화면에 그대로 노출)과 관리자 화면, 코드 주석(`legal_documents_seed.go`) 세 군데 모두에 동일한 경고를 명시. 로컬 검증: 약관 미동의/전화번호 형식오류 시 각각 거부, 정상 동의 시 `users.phone_number`+`terms_agreements` 기록 확인, 관리자가 새 버전 발행 시 공개 API에 즉시 반영되고 이전 버전은 비활성 이력으로 남는 것 확인, 헤드리스 브라우저로 체크박스 미체크 시 제출 버튼 비활성화→둘 다 체크 시 활성화→전체 가입 완료(`#/`로 이동)까지 end-to-end 확인
- **AI 분석 한도 차감 정책 전면 변경(폐기 후 교체)**: "실패해도 API를 호출만 했으면 한도가 차감된다"던 기존 정책(2026-07-31 확정, 8-02 재확인)을 사용자가 명시적으로 폐기하고 "실패는 어떤 이유든 절대 한도를 차감하지 않는다"로 교체. `countAIAnalysisThisMonth`(billing.go)가 이제 `extraction_status='success'`인 행만 세도록 조건 추가 — `checkAIAnalysisQuota`/`#/ai-usage`의 `usedCount`(이전엔 `len(items)`로 실패까지 세던 버그성 로직이었음, 이번에 바로잡음)/대시보드 요약 카드 전부 이 함수 하나를 공유하므로 한 번에 일관되게 바뀜. 실패 이력 자체(items 목록, 배지, 실패사유)는 그대로 다 보여준다 — 안 세는 건 오직 "이번달 사용량" 숫자뿐. **비용 남용 방지**: 한도가 더 이상 방어막이 안 되므로(실패는 무한정 시도해도 공짜), 같은 조직·같은 파일 해시로 최근 1시간 안에 3회 이상 실패하면 그 파일에 한해 신규 업로드든 재시도든 차단(`checkFileRetryRateLimit`, 429 `file_retry_blocked`) — 배치/타이머 없이 "최근 1시간 안의 실패 횟수"를 조회 시점마다 다시 세는 방식이라 오래된 실패가 자연히 창(window) 밖으로 밀려나면 저절로 재시도가 풀린다. 문구도 전부 갱신: "이번 시도는 AI 분석 한도에서 차감되었습니다" 표현을 전부 제거하고 실패 메시지를 "분석에 실패했습니다. 직접 입력해주세요. (한도가 차감되지 않았습니다)"로 통일. 로컬 검증: 같은 파일로 3회 연속 실패시켜 한도(`usedCount`)가 0에서 전혀 안 움직이는 것 확인, 4번째 시도는 429로 차단되는 것 확인, 다른 파일(다른 해시)은 차단 안 되는 것 확인, DB에서 한 건을 success로 표시해 그 건만 정확히 usedCount에 반영되는 것(1건, items는 계속 4건 전부 표시) 확인
- 관리자 회원관리 기능 확장 4종. (1) **플랜별 한도/가격 관리자 조정**: `system_settings`에 9개 키(파이프라인/AI분석 한도, 가격, Business 팀원 한도) 추가, `#/admin/plan-settings` 화면에서 숫자입력으로 수정하면 재배포 없이 즉시 반영(`effectivePlanInfo`가 결제/한도체크/요금제화면 등 `billing.Plans` 직접참조하던 모든 지점을 대체). Business만 있던 팀원 한도(하드코딩 3명)도 `billing.PlanInfo.MaxTeamMembers` 필드로 승격해 같은 방식으로 조정 가능. 가격까지 포함하기로 사용자가 명시적으로 확인(체크아웃~결제승인 사이 가격이 바뀌면 극히 드물게 결제거부(amount_mismatch)가 날 수 있는 리스크는 감수 가능한 수준으로 판단해 그대로 둠). (2) **회원 상세 정보 확장**: 관리자 회원 상세 화면에 구독상태, 이번달 AI분석/파이프라인 생성 건수, 담당자(company_contacts) 목록, 팀원 목록 추가. (3) **회원 탈퇴 처리**(`POST /api/admin/members/{id}/deactivate`): 계정 비활성화(이메일 익명화 + 비밀번호/전화번호 삭제, `users.deactivated_at` 표식)하되 결제이력·감사기록·조직 데이터는 유지(완전삭제 아님). OAuth 재가입 우회를 막기 위해 `user_oauth_identities`는 일부러 안 지우고 `handleLogin`/`handleOAuthCallback` 양쪽에 명시적 차단 체크 추가. 조직 소유자이고 다른 팀원이 남아있으면 탈퇴를 차단(사용자가 확인한 정책 — 오너 없는 조직을 만들지 않기 위함). 전체 대상 일괄 탈퇴는 이번 범위에서 제외(요청사항). (4) **개별 회원 AI 한도 임시조정**(`PUT /api/admin/members/{id}/ai-limit`): `company_profiles.custom_ai_analysis_limit`(+월/사유 컬럼)로 이번 달에만 적용되는 한도 오버라이드 — 다음 달이 되면 별도 배치 없이 조회 시점 비교만으로 자동 원복(사용자가 확인한 정책). 사유 필수, 변경 이력은 `audit_logs`에 기록. 로컬 검증: 플랜 설정 변경이 결제설정/한도체크에 즉시 반영되는지, 개별 한도조정이 같은 조직에는 적용되고 다른 조직에는 영향 없는지+지난달로 강제 설정 시 자동 원복되는지, 팀원 있는 owner 탈퇴 차단(409)과 일반 팀원 탈퇴 성공(로그인 차단+이메일 익명화+조직 데이터 유지+감사로그 기록) 전부 확인
- AI 분석(증빙서류 업로드) 성공/실패 이력을 DB에 남기고 사용내역 화면에서 확인 가능하게 개선 — 이전엔 실패해도 한도가 차감된다는 사실은 업로드 시점 에러 메시지에만 잠깐 보이고 DB엔 아무 기록이 안 남아, 사후에 어떤 시도가 실패했는지 확인할 방법이 없었음(재확인 조사에서 발견). `company_documents`에 `extraction_status`('success'/'failed', NULL='처리중')·`failure_reason`(사용자 친화적 문구, API 원본 에러 텍스트는 절대 노출 안 함 — `classifyExtractionFailureReason`이 "claude api error"/네트워크 에러류는 "일시적 오류", "parse tool input"/"no tool_use block"류는 "문서 인식 실패"로 매핑) 컬럼 추가. 6개 업로드 카테고리(면허·인증/재무제표/실적증명/인력/지식재산권/4대보험) 전부 Claude 호출 직후 이 컬럼을 갱신(`markDocumentExtractionResult`). `#/ai-usage` 화면: 항목마다 성공/실패/처리중 배지 + 실패 시 failure_reason 표시 + 실패 항목에 "재시도" 버튼 추가. 재시도는 디스크에 이미 저장된 원본 파일을 재사용해 새 멀티파트 업로드 없이 바로 재분석하되, 새로운 시도이므로 `company_documents`에 새 행을 만들어 한도도 새로 차감(원래 실패 행은 이력으로 그대로 남김) — 재시도 성공 시 그 자리에서 바로 해당 카테고리의 확인/저장 폼(기존 업로드 플로우와 동일한 컴포넌트 재사용)을 띄워 한 번 더 업로드하지 않고도 저장까지 이어짐. 로컬 검증: `ANTHROPIC_API_KEY` 미설정 환경(로컬 기본 상태)에서 실제 업로드 실패를 발생시켜 DB에 `failed`+사용자 친화적 사유가 정확히 기록되는지, `#/ai-usage`에 배지·사유·재시도 버튼이 뜨는지, 재시도 클릭 시 새 행이 추가되고 한도가 다시 차감되는지(2/60→3/60) 헤드리스 브라우저로 확인 후 테스트 데이터 삭제
- 홈 배너 시드 문구도 브랜드명 변경에 실시간으로 따라가도록 개선 — 바로 전 항목에서는 배너가 "시딩 시점 값 고정"이라 이후 브랜드명을 바꿔도 이미 만들어진 배너는 안 바뀌었는데, 사용자가 완전한 실시간 반영을 요청해 토큰 치환 방식으로 재구현. 배너 제목에 `{brand_name}` 리터럴 토큰을 저장해두고(`ensureBannersTable` 시드값 변경), 공개 조회 API(`GET /api/banners`, `handleListBanners`)가 응답할 때마다 그 순간의 `company_info.brand_name`으로 치환해서 내려줌 — 관리자 화면(`GET /api/admin/banners`)은 원본(토큰 그대로)을 보여줘서 관리자가 토큰의 존재를 알아보고, 직접 다른 문구로 고치면 그 순간부터 고정 텍스트가 되는 것도 이해할 수 있게 안내 문구 추가. 이미 예전 방식(고정 문자열)으로 시드가 끝난 기존 DB를 위해 `ensureBannersBrandNameTokenBackfill` 마이그레이션도 추가 — 그 정확한 옛 문구와 일치하는 행만 토큰으로 바꿔치기하고(관리자가 이미 손댄 배너는 문구가 달라 매칭 안 되므로 건드리지 않음), company_info와 banners 둘 다 있어야 하므로 마이그레이션 순서 맨 마지막에 배치. 불필요해진 이전 방식(company_info를 banners보다 먼저 만들던 Apply() 순서 변경, 시드 시점 서브쿼리)은 되돌림. 로컬 검증: 기존 시드가 있는 DB에서 백필 후 관리자 API로 원본이 토큰으로 바뀐 것 확인, 브랜드명을 바꿨을 때 배너를 전혀 손대지 않아도 공개 API 응답과 실제 홈 화면 배너 오버레이(헤드리스 브라우저로 확인) 둘 다 새 브랜드명으로 즉시 바뀌는 것 확인, 완전히 빈 DB로도 시드→치환이 정상 동작하는 것 확인
- 브랜드명 반영 범위 확대 — 직전 브랜드명 관리 기능에서 의도적으로 남겨뒀던 3곳도 마저 동적으로 전환. (1) 웹푸시 서비스워커의 알림 제목 fallback: 서버가 title을 안 실어 보낸 경우(사실상 거의 안 타는 경로)에도 하드코딩 대신 공개 `/api/company-info`를 직접 불러 최신 브랜드명 사용(진짜 네트워크 실패 때만 최후 하드코딩 기본값). (2) 랜딩페이지 스크린샷 alt 텍스트 2곳(히어로/스포트라이트 대시보드 이미지)에 브랜드명이 들어가는 부분을 `data-brand-alt-suffix` 속성 + `applyBrandName()` 확장으로 동적 치환(초기 렌더와 브랜드명 변경 후 둘 다 갱신). (3) 홈 배너 시드 문구("OO, 지금 시작하세요")가 시딩 시점의 `company_info.brand_name`을 그대로 읽어가도록 변경(`ensureBannersTable`을 `ensureCompanyInfoTable` 뒤로 이동해 순서 의존성 해결) — 단, 이건 최초 1회 시드값이라 이후 관리자가 브랜드명을 바꿔도 이미 생성된 배너 행은 자동 갱신 안 됨(배너는 원래 `#/admin/banners`에서 언제든 직접 수정 가능한 콘텐츠라 의도된 동작). 로컬 검증: 빈 DB로 새로 마이그레이션을 돌려 배너 시드가 그 시점 브랜드명을 정확히 반영하는지 확인, 헤드리스 브라우저로 브랜드명 변경 후 두 스크린샷 alt 속성이 즉시 바뀌는 것과 서비스워커의 fetch 로직이 최신 브랜드명을 정확히 가져오는 것 확인
- 브랜드명(현재 가칭 "공공사업 AI 비서")을 관리자 화면에서 한 곳만 고치면 사이트 전체에 반영되도록 추가 — `company_info` 테이블(직전 커밋에서 신설한 회사정보 싱글턴)에 `brand_name` 컬럼 추가(NOT NULL, 나머지 7개 필드와 달리 필수값, 서버가 빈 값 저장 요청은 `brand_name_required`로 거부). `#/admin/company-info` 폼 맨 위에 별도 "브랜드명(필수)" 카드로 분리 배치. 프론트는 `.brand-name-text` 클래스 마커 + `applyBrandName()`/`loadBrandName()`으로 브라우저 탭 제목, 헤더/사이드바 로고, 랜딩페이지 로고·푸터 저작권 표시를 부팅 시점과 저장 직후 모두 갱신. `manifest.json`은 정적 파일을 없애고 요청마다 DB에서 브랜드명을 읽어 만들어내는 핸들러(`GET /manifest.json`, 정적 서빙보다 우선하는 정확 경로)로 전환해 홈 화면 추가 시 뜨는 이름도 즉시 반영. 의도적으로 그대로 둔 곳(가칭이 남아있어도 사용자 눈에 거의 안 띄거나 이미 별도 경로로 편집 가능): 서비스워커의 푸시 알림 제목 기본값(서버가 항상 명시적 제목을 실어 보내 사실상 안 쓰임), 스크린샷 alt 텍스트 2곳(접근성 텍스트일 뿐 브랜드 아이덴티티 요소 아님), 배너 시드 문구(이미 `#/admin/banners`에서 별도 편집 가능한 마케팅 카피). 로컬 검증: 빈 값 저장 거부(클라이언트+서버 이중 검증), 관리자 UI로 실제 값 변경 후 헤드리스 브라우저로 탭 제목/헤더/사이드바/랜딩 로고/푸터/`manifest.json` 응답까지 즉시(재배포 없이) 반영되는 것 확인, 테스트 후 기본값으로 원상복구
- 랜딩페이지 푸터 회사정보를 관리자 화면에서 편집 가능하게 추가 — `company_info` 싱글턴 테이블(항상 id=1 한 행, `CHECK(id=1)`로 강제) 신설, 공개 `GET /api/company-info` + 관리자 전용 `PUT /api/admin/company-info`. `#/admin/company-info`에 8개 필드(상호명/사업자등록번호/대표자명/주소/대표전화/이메일/제휴및제안이메일/특허출원번호) 입력폼 추가. 랜딩페이지 푸터는 항목별로 비어있으면(빈 문자열은 저장 시 NULL로 정규화) 라벨까지 통째로 숨기고, 7개 필드(제휴이메일 제외) 전부 비어있으면 회사정보 블록 자체를 숨김(CSS `:empty`) — 제휴이메일은 별도로 기존 "제휴 문의" 카드에 조건부 표시. 이용약관/개인정보처리방침 링크는 기존 "준비중" 상태 그대로 유지. 로컬 검증: 전부 비었을 때/일부만/전체 8개 채웠을 때 각각 헤드리스 브라우저로 푸터 렌더링 확인, 관리자 화면에서 UI로 직접 값 수정 후 별도(비로그인) 브라우저 컨텍스트에서 재배포 없이 즉시 반영되는 것까지 확인
- 랜딩페이지 디자인 고도화 — (1) 실제 제품 화면 스크린샷 3장(홈 대시보드/파이프라인 칸반/성장분석) 삽입 — 헤드리스 브라우저로 실제 로그인 화면을 촬영해(`collector/internal/webui/static/screenshots/`) 목업이 아닌 진짜 UI로 신뢰도 확보, 맥 스타일 브라우저 프레임(점 3개 창틀) 공용 컴포넌트로 감쌈. (2) 핵심 기능 중 화면이 있는 3개(오늘의 업무/팀 협업/성장분석)를 "제목+설명+스크린샷" 스포트라이트 섹션으로 좌우 번갈아 크게 배치, 나머지 3개(서류체크리스트/마감알림/캘린더연동)는 기존 카드 그리드 유지. (3) 히어로 섹션에 대시보드 스크린샷을 perspective+rotate로 기울여 브랜드 블루 glow와 함께 "떠 있는" 느낌으로 배치(900px 이하에서는 기울임 해제). (4) 섹션 배경을 흰색/연한 블루틴트/연한 회색 3색으로 번갈아 적용해 스크롤 구간을 명확히 구분. (5) 섹션 상하 여백을 기존 대비 약 2배(랜딩 전용 값, 앱 전역 --space-xl은 그대로 둠)로 확대. 로컬 검증: 스크린샷 촬영을 위해 파이프라인 테스트 데이터를 임시 보강(검토전/참여검토/승인대기/준비중/제출완료 고르게 분포) 후 촬영·크롭하고 원상복구, 헤드리스 브라우저로 히어로/스포트라이트 좌우배치/모바일 반응형 확인
- 비로그인 첫 화면을 마케팅 랜딩페이지로 전면 개편 — 지금 앱(로그인 후 대시보드)과 완전히 독립된 레이아웃(`body.is-landing`으로 사이드바/헤더/탭바/전역 프로필 배너를 숨기고 랜딩 전용 헤더+푸터만 표시, `@media print`이 인쇄 시 앱 셸을 숨기는 것과 같은 원리). 히어로/문제제기(500건→7건)/핵심기능 6종/활용 시나리오 3종(예시 명시)+제휴문의/FAQ 아코디언 5문항(환불정책은 실제 로직 그대로 요약, AI 정확도는 "참고용 1차 필터, 확정판정 아님" 명시)/요금제 4단(`/api/billing/config` 재사용, `#/billing/plans`와 동일 출처)/푸터(회사정보는 전부 `[]` 플레이스홀더, 이용약관·개인정보처리방침 링크는 준비중 처리) 순서로 구성. 스크롤 시 섹션이 부드럽게 나타나는 효과는 외부 라이브러리 없이 IntersectionObserver+CSS transition으로 구현(prefers-reduced-motion 존중). 기존 `renderMarketingHero`(최소 히어로 카드)를 완전히 대체
- 콘텐츠 관리 시스템(CMS) 1단계 — 홈 화면 배너 슬라이드 + 프로필 미완성 안내 배너. `banners` 테이블 신설(공개 GET /api/banners, 노출기간/활성화/순서 지원), 자동 넘김+좌우 화살표+점 인디케이터를 순수 CSS/JS로 구현(외부 라이브러리 없음), 초기 배포 시 브랜드 톤(네이비/블루) SVG 플레이스홀더 3장 자동 시드. 관리자 CMS(배너 관리/회원 알림/팝업/공지게시판) 3~6단계는 다음 단계로 진행 예정
- CMS 1단계 피드백 반영 — (1) 배너 슬라이드에 "오늘 하루 보지 않기" 닫기(×) 버튼 추가(localStorage에 오늘 날짜 저장, 팝업 관리(6단계 예정)와 동일한 방식). (2) 프로필 미완성 안내를 홈 화면 전용에서 `#global-profile-banner`(#app 밖, route()가 갈아끼우지 않는 영역)로 옮겨 모든 화면 최상단에 sticky로 항상 노출되게 변경, 완성도% 조회도 `/api/me/profile-completeness`를 직접 재사용하도록 전환(페이지 종속성 제거), 배경을 옅은 틴트 대신 꽉 찬 색(파랑/danger 빨강)으로 강조
- 배너를 페이지 인라인 캐러셀에서 홈 진입 시 뜨는 오버레이 레이어(공중에 뜬 팝업형)로 전환 — 화면 전체를 반투명 배경으로 덮어 뒤 콘텐츠 클릭을 막고(배경 클릭으로는 안 닫힘, 닫기 X 버튼으로만 닫힘), 열려있는 동안 배경 스크롤도 잠금. "오늘 하루 보지 않기" 로직은 그대로 유지
- 콘텐츠 관리 시스템(CMS) 3~6단계(관리자 CRUD 4종) 완료 — (3) 배너 관리 `#/admin/banners`: 목록/등록/수정/삭제 + 화살표로 노출순서 변경(`display_order` 인접값 swap) + 이미지 업로드(매직바이트 검증, 해시 기반 dedup 저장, 새 공개 정적 서빙 라우트 `/cms-images/{filename}`). (4) 회원 알림 메시지 `#/admin/broadcast`: 전체/플랜별 대상 선택 + 이메일·인앱·웹푸시 중복선택 즉시 발송(기존 알림 인프라 재사용, Free 이메일 한도는 적용 안 함 — 운영성 공지 성격) + 발송 이력. (5) 팝업 관리 `#/admin/popups`: `popups` 테이블 신설, 홈 진입 시 배너보다 우선 노출되는 오버레이, "오늘 하루 보지 않기"는 팝업별 localStorage 키(`popup_dismissed_{id}_{date}`)로 관리, 간단 마크다운(굵게/줄바꿈/URL 자동링크)만 지원. (6) 공지 게시판 `#/admin/announcements` + 사용자용 `#/announcements`: `announcements` 테이블 신설, 상단고정/조회수. 사이드바에 "공지사항" 메뉴 추가(9번째 항목). 배너/팝업 오버레이가 홈이 아닌 화면으로 이동해도 안 사라지던 버그를 route()에서 정리하도록 수정
- 공지 게시판 등록/수정 폼에 조회수 직접 지정 필드 추가 — 등록 시 초깃값(기본 0) 지정 가능, 수정 시에도 값 직접 변경 가능(음수는 거부). 상세 열람 시 자동 증가하는 기존 동작과는 독립적으로 관리자가 언제든 덮어쓸 수 있음
- Phase 3(파이프라인 칸반/표) 구현 — `#/pipeline`에 리스트/칸반/표 뷰 토글 추가. 칸반은 진행중 4상태 컬럼 + 드래그앤드롭(실패 시 롤백) + 종결 5상태 접이식 섹션, 표는 8개 컬럼 정렬/상태필터. AI판정등급을 파이프라인 목록 API에 추가(실시간 계산, growth_analytics와 동일 방식). 모바일은 토글 숨김+리스트 강제(라이브 리사이즈도 반응)
- Phase 4(서류자동화 고도화) 2단계 — 배치당 처리 상한(200건) 추가로 대량 백로그 시 API 비용/시간 폭주 방지, AI 보완 호출 영구 실패 재시도 상한(3회) 추가로 무한 재시도 방지, 관리자 화면에 배치 성공/실패/포기 건수 표시
- Phase 5(알림 완성) 2단계 — 관리자 화면에 알림 발송 실패 이력 표시(이전엔 서버 로그로만 남음), 주간/월간 리포트를 인앱 알림함에도 반영(이전엔 이메일만), 팀 초대 수락 시 오너에게 알림(인앱+이메일) + 팀 초대 발송도 다른 알림과 동일하게 발송 이력에 기록
- Phase 6(웹푸시/PWA) — "앱" 대신 웹 푸시 알림 + 홈 화면 설치(PWA)로 구현. manifest.json/service worker 추가, 구독 단위는 회원(로그인 계정), 마감리마인더/상태변경/추천다이제스트 3종에 푸시 연결(webpush-go, 이 프로젝트 첫 신규 외부 Go 의존성). iOS는 홈 화면 추가 안내 배너로 대응. 관리자 대시보드 알림 실패 이력에는 아직 미포함(다음 단계 후보)
- Phase 7(성장분석 고도화) — 완성도·파이프라인 성과 추이를 표에서 순수 SVG 선그래프로 전환, AI등급분포를 실시간 재계산뿐 아니라 리포트 생성 시점 스냅샷으로도 저장해 진짜 시계열 추이 차트 추가(리포트 2건 이상부터 표시)
- Phase 6 추가 — 서비스워커에 앱 쉘 오프라인 캐싱(오프라인 시 로그인 화면 등 골격은 뜨도록, API 응답은 캐시 안 함), SMS 알림을 유료 플랜 전용으로 제한(Free는 이메일+웹푸시만, 발송 직전 플랜 확인)
- Phase 4 PDF 텍스트 추출 Go 이식 조사 — 실제 나라장터 PDF로 검증한 결과 순수 Go PDF 라이브러리 4종(unipdf/go-fitz/pdfcpu/ledongthuc) 전부 실사용 불가로 판단(라이선스/cgo/API부재/좌표버그), 이식 보류·PDF도 HWP처럼 Python 수동 단계로 유지(코드 변경 없음, document_extraction.go 주석에 근거 기록)
- Phase 7(성장분석 고도화) 2단계 — 추이 차트에 기간 필터(전체/6개월/3개월) 추가, 벤치마킹 신설(프로필 완성도·파이프라인 전환율을 전체 가입 회사 익명 집계 평균과 비교, 비교 대상 5개 미만이면 비표시로 익명성 보호), 낙찰이력(scsbid) 연동 대비 업종별 평균 낙찰률 백엔드 구조 추가(데이터 없으면 응답에서 자동 생략, UI 변경 없음)
- 플랜별 월간 알림성 이메일 발송 한도 추가 — Free 플랜은 월 20건(마감리마인더/상태변경/추천다이제스트/주간·월간 리포트만 집계, 회원가입 인증·팀 초대 등 필수 이메일은 무관하게 항상 발송), 초과분은 이메일만 조용히 스킵하고 인앱/웹푸시/SMS는 그대로 발송(다음달 1일 자동 리셋, AI분석 한도와 동일한 롤링 윈도우 방식). 한도는 하드코딩 대신 새 system_settings(key-value) 테이블에 저장해 관리자 대시보드 "플랜 설정" 섹션에서 재배포 없이 즉시 조절 가능. 관리자 대시보드에 이번달 한도 초과 스킵 건수 타일 추가
- 간편로그인(구글/네이버/카카오) 추가 — 표준 OAuth 2.0 Authorization Code 흐름(GET /api/auth/{provider}/start·callback, state 쿠키로 CSRF 방지). 새 user_oauth_identities 테이블(users에 provider 컬럼을 직접 두지 않고 분리 — 한 계정에 여러 소셜 계정 동시 연결 가능), users.password_hash는 NULL 허용으로 완화(소셜 전용 계정 대응, handleLogin은 이 경우 social_login_only 에러 반환). 같은 이메일의 기존 계정이 있으면 자동 연결, 없으면 신규가입(회사 정보 미등록 시 온보딩 2단계로 리다이렉트). 로그인/회원가입 화면에 각 서비스 공식 브랜드 색상의 버튼 추가. GOOGLE_CLIENT_ID/SECRET, NAVER_CLIENT_ID/SECRET, KAKAO_REST_API_KEY(+선택 KAKAO_CLIENT_SECRET) 미설정 시 해당 버튼 클릭이 404를 반환할 뿐 서버는 정상 기동(다른 외부 서비스 키와 동일한 관례) — 실제 키 발급 전이라 구조/컴파일 검증까지만 완료, 발급 후 엔드투엔드 테스트 예정

## 2026-08-01
- 최종관리자(system_admin) 대시보드 신설 (#/admin)
- 결제 내역 화면 추가, 플랜 변경 정책(즉시 업그레이드 / 예약 다운그레이드) 확정
- 성장분석(3단계) 추가 — 완성도·파이프라인 성과 추이, AI등급분포, ROI
- 기업마당/지자체 지원사업 수집기 추가 (2단계) + 판정 로직 분리
- 주간/월간 자동 리포트 추가
- 경쟁사/낙찰이력 기능(낙찰정보서비스 수집기 포함) 추가
- 팀기능 추가 — 회사 프로필을 1:1에서 다중 사용자 조직 구조로 전환
- 토스(Toss) 스타일 디자인 리뉴얼 — 디자인 토큰 전체 반영, 홈/공고검색/진행중사업/캘린더/기업프로필/성장분석/관리자 화면 구조 개편(행 리스트, 도넛차트 등)
- 공고 검색 정렬 옵션(신규/이름/예산/상태/마감순) + 묶음별 건수 표시 추가
- 인앱 알림함 신설(#/notifications) — 마감 리마인더/상태변경/추천다이제스트를 앱 안에서 직접 확인
- 서류자동화 고도화(Phase 4) — 공고→제출서류/자격조건 규칙기반+AI 보완 추출을 apiserver 자동 배치로 전환(기존 사람이 수동 CLI로 돌리던 것)
- 결제 환불/해지 기능 추가 — 이번 결제 주기 미사용 시 전액환불(토스 결제취소 API 연동), 사용 이력 있으면 해지 신청(만료일까지 이용 후 자동 Free 전환)

## 2026-07-31
- 파이프라인 상태 단순화 + 자동전환 도입
- 증빙서류 종류 17종으로 확대, 지식재산권 테이블 신설
- 공고 목록 무한스크롤 페이지네이션 추가
- SMS/알림톡(알리고) 알림 채널 추가
- 참여판단 PDF 보고서 기능 추가
- 디자인 리뉴얼(공통 컴포넌트, PC 사이드바, 모바일 탭바 등 전 화면 통일)
- 토스페이먼츠 구독 결제 연동
- 모바일 반응형 대응, 캘린더 연동(.ics), 3분 이내 온보딩으로 개선
- 이메일 알림 기능 추가
- 업무자동화 참여 파이프라인 + 증빙서류 종류 확장
- 프로필 완성도/신뢰도 요약 + AI 참여분석 동적 질문 힌트 추가

## 2026-07-30
- 참여 가능성 판정등급 5단계(참여권장/조건부/공동수급검토/확인필요/참여곤란) 도입
- 재무정보/수행실적/인력정보 구조화 추가
- 공고 변경사항이 참여 가능성에 미치는 영향 자동 설명 기능 추가
- 면허·인증 구조화 + 증빙서류 업로드 AI 자동추출 기능 추가
- AI 요약 브리핑(핵심 3줄 요약) 추가
- 관심공고 추적(북마크) 기능 추가
- "공고 보여주기" 플랫폼에서 "참여판단 AI 비서"로 방향 전환
- 자격조건/제출서류 AI 기반 보완 추출(Claude 연동) 추가
- 공고 상세 검토(승인/반려) 워크플로우 추가
- 웹 화면 디자인을 전문 컨설팅 업체 톤으로 전면 개편

## 2026-07-29
- 프로젝트 초기 구축 — 데이터모델, Go 수집엔진, PostgreSQL 연동, API 서버
- 정적 웹 UI 도입 (빌드 없는 단일 HTML)
- 회원가입/로그인/기업프로필 기능 추가
- 나라장터(G2B) 입찰공고정보서비스 실연동 + 첨부파일 다운로드
- 기업 적합성 판정 규칙 엔진 1차 버전 추가
- Python 문서분석(analyzer) 파이프라인 도입 — 첨부파일 텍스트 추출 + 구조화 추출
